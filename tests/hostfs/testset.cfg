from gvtest import *


def testset_build(testset):

    testset.set_name('hostfs')

    # The host (semi-hosting) file system is reached through the board BSP's
    # "/host" mount point (targets that provide it set the 'hostfs' capability).
    # RTL testbenches do not serve semi-hosting calls, so the first host file
    # access would spin in the ebreak exception handler forever.
    if testset.get_target().get_property('hostfs') and testset.get_platform() != 'rtl':
        testset.import_testset(file='hostfs_rw/testset.cfg')
